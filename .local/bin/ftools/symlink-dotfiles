#!/usr/bin/env bash

cd ~
bash ~/.dotfiles/.local/bin/ftools/stowsh ~/.dotfiles -s

# Stowsh is a fucking strangely written script and I cannot even if my life depended on it figure out a way to ignore files.
# I'll just do this shit instead fuckin shit
rm -rf ~/.git
rm -rf ~/.gitignore
rm -rf ~/profiles
rm -rf ~/colorschemes
rm ~/git-repo-paths
rm ~/README.md
