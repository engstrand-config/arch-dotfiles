#!/bin/bash

# Replaces a placeholder in a file with a new value
# If $2 is not defined, $3 is used as fallback
if [ -z "$2" ] || [ "$2" == "" ]; then
  sed -i "s|$1|$3|g" $4
else
  sed -i "s|$1|$2|g" $4
fi
