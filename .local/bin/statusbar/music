#!/bin/sh

# color_paused="^c$(xgetres critical)^"
# info=$(mpc)
# paused=$(echo "$info" | grep -c paused)

# $(echo "$paused" > 0) && printf "$color_paused"; printf "$song^d^"
info=$(mpc)
paused=$("$info" | grep -c paused)

echo $paused > 0 && printf ^c(xgetres critical)^; "$info"
