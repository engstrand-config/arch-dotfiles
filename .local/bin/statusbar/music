#!/bin/sh
info=$(mpc)
playing=$(grep "playing" <<< "$info" )
title=$(echo "$info" | sed 1q)
if [ -z "$playing" ]; then
  printf ^c$(xgetres critical)^
  printf " "
else
  printf " "
fi

printf "$title"
echo ^d^
