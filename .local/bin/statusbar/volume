#!/bin/sh
[ "$(pulsemixer --get-mute)" = "1" ] && echo "^c$(xgetres critical)^^d^" && exit

vol=$(pulsemixer --get-volume | awk '{print $1}')

if [ "$vol" -ge "50" ]; then
	icon=""
elif [ "$vol" -eq "0" ]; then
	icon=""
else
	icon=""
fi

echo "$icon $vol %"
