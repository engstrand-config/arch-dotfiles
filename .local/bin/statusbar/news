#!/bin/sh

case $BLOCK_BUTTON in
	1) ddspawn newsboat >/dev/null &
esac

UNREAD=$(</tmp/newsupdate)

# This step is not really needed, but the icon is always visible otherwise...
# think of some better solution
if [ -z "$UNREAD" ]; then
	exit
fi

printf "<span color='$WAL_LIGHT'>$UNREAD</span>\n"
